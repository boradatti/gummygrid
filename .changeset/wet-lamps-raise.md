---
'gummygrid': minor
---

- allow choosing btw "portable" and "web" output formats for SVGs ("portable" is default, better fit when there's a need for rendering the svg in a non-browser environment, e.g. with the sharp library)
- allow setting custom svg output size
